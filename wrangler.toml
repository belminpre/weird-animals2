# Cloudflare Pages + custom _worker.js (advanced mode)
# Deploy: npm run deploy:pages  (builds, copies _worker.js into dist, then pages deploy)

name = "weird-animals"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"
