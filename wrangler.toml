# Cloudflare Workers + Assets (_worker.js + static site in dist)
# Deploy: npm run build && npx wrangler deploy
# Local dev: npm run build && npx wrangler dev

name = "weird-animals"
compatibility_date = "2024-01-01"
main = "_worker.js"

[assets]
directory = "./dist"
